#include<stdio.h>
int main()
{
    int a,b,c,d,n;
    int x,y;
    scanf("%d",&n);
    while(n>0)
    {
        scanf("%d%d",&a,&b);
        scanf("%d%d",&c,&d);
        if()
    }
    return 0;
}
