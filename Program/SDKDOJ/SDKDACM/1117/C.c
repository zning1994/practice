#include<stdio.h>
#include<stdlib.h>
#include<string.h>
#include<math.h>
int main()
{
    int a,b,c,d,e,f;
    int sum=0;
    while(scanf("%d%d%d%d%d%d",&a,&b,&c,&d,&e,&f))
    {
        if(a==0&&b==0&&c==0&&d==0&&e==0&&f==0)
            break;
        else
        {
            if(f!=0)
                sum+=f;
            if(a!=0)
            {
                if(a!=36){
                    num=36-a;
                if(num%2==0)
                    num}
                    else
                }

        }
        return 0;
    }
