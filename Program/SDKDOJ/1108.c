#include<stdio.h>
#include<string.h>
int main()
{
    char s[1100];
    int N;
    while(scanf("%d",&N))
    {
        if(N==0)
            break;
        else
        {
            while()
            scanf("%s",s);
        }
    }
    return 0;
}
