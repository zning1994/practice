#include <stdio.h>
#include <stdlib.h>
#include <math.h>
typedef struct{       int maxNum; //适用于表的最大数
char *table;    //素数表储存从3到maxNum间的奇数和与之对应的是否是素数的位标记           //如3为素数，则table[0]第一位为1，4为偶数不存入数组，9为素数则table[0]第四位为0           //数组的一个元素储存8个奇数的素数状态   }SieveTable;          SieveTable *CreateTable(int n);   void DestroyTable(SieveTable *table);   void PrintTable(SieveTable *table);   int IsPrime(int num,SieveTable *table);          int main(void)   {       while(1)       {           int n;           int num,i;           SieveTable *t;                  system("CLS");           printf("输入创建大小  ");           scanf("%d",&n);           t=CreateTable(n);           printf("输入10个素数测试\n");           for(i=0;i<10;i++)           {               scanf("%d",&num);               switch(IsPrime(num,t))               {               case 1:                   printf("%d是素数\n",num);                   break;               case 0:                   printf("%d不是素数\n",num);                   break;               case -1:                   printf("%d超出范围\n",num);                   break;               }           }           printf("打印素数表\n-------------------------------\n");           PrintTable(t);                          printf("\n任意键清除素数表");           system("pause>NUL");           DestroyTable(t);           printf("\n素数表已清除\n");           system("pause");       }              return 0;   }
